# Protoplanetary Disk Simulator

Watch dust particles orbit a star, collide, and merge into planets. Click to spawn new particles, scroll to zoom. An N-body gravity sandbox showing emergent structure formation.

Build: `gcc -o sim main.c -lSDL2 -lm`
